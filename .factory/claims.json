[
  {
    "id": "cue-workflow",
    "claim": "Imports local audio, marks a cue, exports JSON, and restores the saved cue after reload.",
    "where": "product, README",
    "test": "npm run test:e2e -- --grep @claim:cue-workflow",
    "sandbox": "fresh browser context with a generated WAV; assert cue creation, JSON download, reload, and no console errors"
  },
  {
    "id": "offline-reload",
    "claim": "A saved set works offline after the first visit.",
    "where": "landing facts, README",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "dedicated browser.newContext; save generated WAV, wait for the service worker, set offline, reload, and assert the studio and offline state"
  },
  {
    "id": "local-privacy",
    "claim": "Audio stays in the browser and is not uploaded.",
    "where": "landing facts, privacy, README",
    "test": "npm run test:e2e -- --grep @claim:local-privacy",
    "sandbox": "fresh demo context with all requests recorded; assert no network request leaves the product origin"
  },
  {
    "id": "json-no-audio",
    "claim": "Cue JSON exports do not include audio bytes.",
    "where": "privacy, README",
    "test": "npm run test:e2e -- --grep @claim:json-no-audio",
    "sandbox": "sample demo; parse the downloaded JSON and assert it contains only audio name and duration metadata"
  },
  {
    "id": "free-five",
    "claim": "Cuebook Free keeps up to five cues and asks before shortening a larger import.",
    "where": "landing facts, pricing dialog, terms, README",
    "test": "npm run test:e2e -- --grep @claim:free-five",
    "sandbox": "fresh browser with generated WAV and six-cue fixture; assert confirmation before mutation and retained limit notice"
  },
  {
    "id": "plus-license",
    "claim": "A valid returned Plus license unlocks the paid controls and is removed from the address bar.",
    "where": "pricing dialog, README",
    "test": "npm run test:e2e -- --grep @claim:plus-license",
    "sandbox": "recorded successful Sociobot verification response; assert stored token, clean URL, and unlocked state"
  },
  {
    "id": "three-scenes",
    "claim": "Every user can select Contour, Orbit, and Shards scenes.",
    "where": "product, terms, README",
    "test": "npm run test:e2e -- --grep @claim:three-scenes",
    "sandbox": "sample demo; select each named scene and assert its pressed state"
  },
  {
    "id": "deterministic-scenes",
    "claim": "A scene renders the same frame whenever the playhead returns to the same media time.",
    "where": "product, README",
    "test": "npm run test:e2e -- --grep @claim:deterministic-scenes",
    "sandbox": "sample demo; hash canvas pixels at one timeline position, move away and return, then compare exact pixels"
  },
  {
    "id": "pwa-install",
    "claim": "Cuebook includes an installable standalone PWA shell.",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:pwa-install",
    "sandbox": "fresh browser; parse the manifest, assert standalone display and icons, then assert an activated service-worker controller"
  },
  {
    "id": "plus-recording",
    "claim": "Cuebook Plus imports more than five cues and downloads a WebM rehearsal in supported Chromium browsers.",
    "where": "pricing dialog, README",
    "test": "npm run test:e2e -- --grep @claim:plus-recording",
    "sandbox": "sample demo with a cached valid fixture license; import six cues, then start and stop recording and assert a WebM download"
  },
  {
    "id": "demo-sandbox",
    "claim": "The one-click demo starts with five sample cues and does not change the user's saved set.",
    "where": "landing, demo banner, README",
    "test": "npm run test:e2e -- --grep @claim:demo-sandbox",
    "sandbox": "save a real fixture, enter /demo/, modify and reload the in-memory sample, then return and assert the real fixture is unchanged"
  }
]
